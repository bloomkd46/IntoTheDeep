// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/java
{
	"name": "Android Java",
	// Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile
	"image": "mcr.microsoft.com/devcontainers/java:1-11-bookworm",
	"features": {
		"ghcr.io/nordcominc/devcontainer-features/android-sdk:1": {},
		"ghcr.io/devcontainers/features/github-cli:1": {}
	},
	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	// "forwardPorts": [],
	// Use 'postCreateCommand' to run commands after the container is created.
	// Use "onCreateCommand" becuase it runs commands after the container is created, but before the extensions are installed.
	"onCreateCommand": "./gradlew assembleDebug ; echo Done.",
	// Configure tool-specific properties.
	"customizations": {
		"codespaces": {
			"openFiles": [
				"TeamCode/src/main/java/org/firstinspires/ftc/teamcode/readme.md"
			]
		},
		"vscode": {
			"extensions": [],
			"settings": {
				"java.server.launchMode": "LightWeight",
				"android.sdkLocation": "${ANDROID_SDK_ROOT}"
			}
		}
	}
	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	// "remoteUser": "root"
}